#include <iostream>
#include <vector>
#include <string>

using namespace std;


int main() {
// new way to loop through an array
vector<int> ARRAYNAME(5);
for(auto &d : ARRAYNAME) {}

// declare a string
string aString{"Hello World"};

// fill a string
string aString2;
getline(cin, aString2);

// add to the end of an array
// ARRAYNAME.push_back(VALUE);



}