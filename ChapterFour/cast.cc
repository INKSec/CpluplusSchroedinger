/*
Typecast possibilities

static_cast<TYPE>(expr)

dynamic_cast<TYPE>(expr)
later

const_cast<TYPE>(expr)
deactivates a const deklaration (better don't do this)

reinterpret_cast<TYPE>(expr)
use only with raw bits and binary bytes

forget about old c casts
*/